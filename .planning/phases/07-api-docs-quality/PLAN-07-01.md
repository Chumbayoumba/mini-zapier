# PLAN-07-01: Swagger Documentation Enhancement

> Phase: 7 | Wave: 1 | Requirements: API-01, API-02

## Goal
Complete Swagger documentation with @ApiResponse, examples, and proper schemas on all endpoints.

## Tasks

### T1: Add @ApiResponse decorators to AuthController
- Add 200/201/401 responses with description and schema
- Add @ApiBody examples for login/register

### T2: Add @ApiResponse decorators to UsersController
- Add 200/404 responses
- Document profile endpoint

### T3: Add @ApiResponse decorators to WorkflowsController
- Add 200/201/404 responses for CRUD
- Add pagination schema for list endpoint
- Add 400 for validation errors

### T4: Add @ApiResponse decorators to ExecutionsController
- Add 200 responses with pagination schema
- Add stats endpoint docs
- Add filter query param docs

### T5: Add @ApiResponse decorators to HealthController
- Add 200 response schema

### T6: Add @ApiResponse decorators to WebhookController
- Document the public webhook endpoint
- Note: no auth required

### T7: Create response DTOs for Swagger schemas
- Create `common/dto/pagination-response.dto.ts` with ApiProperty
- Create response wrapper types if needed

## Files
- MODIFY: `auth/auth.controller.ts`
- MODIFY: `users/users.controller.ts`
- MODIFY: `workflows/workflows.controller.ts`
- MODIFY: `executions/executions.controller.ts`
- MODIFY: `health/health.controller.ts`
- MODIFY: `triggers/webhook/webhook.controller.ts`
- CREATE: `common/dto/pagination-response.dto.ts` (if needed)

## Verification
- All existing tests still pass (356)
- Swagger UI at /api/docs shows all response schemas
