# PLAN-07-02: Error Handling + ESLint + REST Polish

> Phase: 7 | Wave: 1 | Requirements: API-03, TST-04

## Goal
Register global exception filter, standardize error responses, configure ESLint for both packages.

## Tasks

### T1: Register AllExceptionsFilter in main.ts
- Import and apply `app.useGlobalFilters(new AllExceptionsFilter(httpAdapterHost))`
- Verify error format matches `{ error: { code, message, details } }`

### T2: Update AllExceptionsFilter response format
- Change from `{ statusCode, message, timestamp, path }` to:
```json
{
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "Human readable message",
    "details": { "field": "email", "issue": "must be valid" },
    "statusCode": 400,
    "timestamp": "ISO string"
  }
}
```

### T3: Create backend ESLint config
- Create `apps/backend/.eslintrc.js` with TypeScript rules
- parser: @typescript-eslint/parser
- Extend: eslint:recommended, @typescript-eslint/recommended
- Rules: no-unused-vars warn, no-explicit-any warn

### T4: Create frontend ESLint config
- Create `apps/frontend/.eslintrc.json` for Next.js
- Extend: next/core-web-vitals, next/typescript

### T5: Fix critical lint issues (if any)
- Run lint, fix auto-fixable issues
- Document remaining warnings

## Files
- MODIFY: `apps/backend/src/main.ts`
- MODIFY: `apps/backend/src/common/filters/all-exceptions.filter.ts`
- CREATE: `apps/backend/.eslintrc.js`
- CREATE: `apps/frontend/.eslintrc.json`

## Verification
- Tests still pass (356)
- Error responses follow new format
- `npm run lint` runs without errors (warnings OK)
