# PLAN-07-03: Test Coverage Boost

> Phase: 7 | Wave: 2 | Requirements: TST-01, TST-02, TST-05
> Depends on: PLAN-07-02 (error filter changes affect test expectations)

## Goal
Increase backend test coverage, add integration tests, ensure all tests pass.

## Tasks

### T1: Run coverage report with increased heap
- `NODE_OPTIONS=--max_old_space_size=4096 npx jest --coverage`
- Identify lowest coverage modules

### T2: Add missing service tests
- Focus on services with <80% coverage
- Priority: WorkflowsService, UsersService, TriggersService

### T3: Add controller tests
- Test HTTP layer: routes, guards, pipes
- Test error responses match new format

### T4: Add integration test for trigger → execution flow
- Test: create workflow → activate → trigger webhook → verify execution created
- Uses supertest + in-memory setup

### T5: Ensure all 356+ tests still pass
- Run full suite
- Fix any broken tests from error format change

## Files
- CREATE/MODIFY: Various `*.spec.ts` files
- MODIFY: Existing tests if error format changed

## Verification
- All tests pass
- Coverage >80% on service layer
